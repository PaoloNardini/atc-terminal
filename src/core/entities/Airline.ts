export class Airline {
    name?: string         // Airline commercial name
    alias?: string        // Airline alias name
    callsign?: string     // Airline radio callsign
    icao?: string         // Airline 3 letters ICAO code
    iata?: string         // Airline 2 letters IATA code
    country?: string      // Airline base country
}